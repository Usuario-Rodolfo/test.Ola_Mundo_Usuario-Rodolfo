#include <stdio.h>  // Biblioteca padrão de entrada e saída

int main() {
    // Declaração e inicialização da variável 'nome'
    char nome[20] = "Usuario-Rodolfo";  

    // Exibe uma saudação personalizada
    printf("Olá, %s!\n", nome);

    // Finaliza o programa
    return 0;  
}
